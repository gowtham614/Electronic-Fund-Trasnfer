The dining philosopher approach is used. Each thread checks the global status of the accounts and takes both the accounts simultaneously if both are available otherwise it waits for the accounts to become available. when the thread finishes the transfer it signals all the other thread which are dependent on the accounts.
